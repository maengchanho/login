apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-service-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: login-service
    http:
      paths:
        - path: /login
          pathType: Prefix
          backend:
            service:
              name: login-service.login-service.svc.cluster.local
              port:
                number: 5006
        - path: /login
          pathType: Prefix
          backend:
            service:
              name: login-service.login-service.svc.cluster.local
              port:
                number: 5006
        - path: /login
          pathType: Prefix
          backend:
            service:
              name: login-service.login-service.svc.cluster.local
              port:
                number: 5006